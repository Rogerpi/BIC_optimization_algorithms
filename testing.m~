addpath('./benchmarks')

fprintf("Start test!\n")

global initial_flag
initial_flag = 0;

%Choose fun_num
func_num = 1;
fprintf("Using function n %n \n",func_num);

%f = @(y) benchmark_func(y,func_num);
f = @(v) 100 * gauss(v(:,1),v(:,2));
alpha = 0.25;
beta = 0.25;
gamma = 0.25;
delta = 0.2;
inf_ratio = 0.5;
epsilon = 0.2;
PSO_standard(100,alpha,beta,gamma,delta,epsilon,inf_ratio,f);


